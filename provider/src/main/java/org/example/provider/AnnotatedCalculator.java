package org.example.provider;

import org.example.service.annotation.Calculation;

@Calculation(value = (12+8))
public class AnnotatedCalculator {
    public int calculateNumbers() {
        return calculateNumbers();
    }
}
